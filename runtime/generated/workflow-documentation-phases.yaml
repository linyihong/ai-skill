---
header:
  generated_from: workflow/documentation/execution-flow.md
  generated_at: '2026-05-15T02:01:01Z'
  compiler_version: 1.0.0
  status: synced
phases:
- name: 0. 釐清讀者與生命週期
  source_line: 5
  allowed_actions:
  - action: "**本輪對話**：優先放 `<PROJECT_ROOT>/.agent-goals/` 或暫存筆記，不要當成長期規範。"
    source: bullet
  - action: "**專案期間**：放 `docs/`、`design/`、`adr/` 等專案內 durable 路徑。"
    source: bullet
  - action: "**組織長期 / 多專案重用**：抽象成與 repo 無關的規則語意；具體 host、ticket、log 留在專案證據區。對照 [`../../enforcement/reusable-guidance-boundary.md`](../../enforcement/reusable-guidance-boundary.md)。"
    source: bullet
- name: 1. 分類維度（寫入前先選標籤）
  source_line: 18
  allowed_actions:
  - action: "`how-to` / `reference` / `policy` / `decision` / `runbook` / `glossary`
      | 決定段落結構與詳略；`policy` 要短而可驗證。"
    source: table
  - action: "`human` / `agent` / `both` | `agent` 為主時必須有「何時讀、讀到哪停」的明確停止條件。"
    source: table
  - action: "`draft` / `active` / `deprecated` | 讓搜尋與 agent 可過濾過期文。"
    source: table
  - action: "`index` / `leaf` | `index` 只連結與摘要；`leaf` 承載細節。"
    source: table
  tables:
  - header:
    - 維度
    - 範例值
    - 用途
    rows:
    - - "`kind`"
      - "`how-to` / `reference` / `policy` / `decision` / `runbook` / `glossary`"
      - 決定段落結構與詳略；`policy` 要短而可驗證。
    - - "`audience`"
      - "`human` / `agent` / `both`"
      - "`agent` 為主時必須有「何時讀、讀到哪停」的明確停止條件。"
    - - "`stability`"
      - "`draft` / `active` / `deprecated`"
      - 讓搜尋與 agent 可過濾過期文。
    - - "`routing`"
      - "`index` / `leaf`"
      - "`index` 只連結與摘要；`leaf` 承載細節。"
- name: 2. 選目錄與檔名（對照 content layering）
  source_line: 31
  allowed_actions:
  - action: "`docs/README.md` 或主題資料夾的 `README.md`"
    source: table
  - action: "`docs/how-to/<topic>.md`"
    source: table
  - action: "`docs/reference/<topic>.md`"
    source: table
  - action: "`docs/adr/` 或 `decisions/`"
    source: table
  - action: "`.agent-goals/` 或 `notes/scratch/`（不當作單一真相）"
    source: table
  tables:
  - header:
    - 意圖
    - 建議路徑形狀
    rows:
    - - 入口與路由
      - "`docs/README.md` 或主題資料夾的 `README.md`"
    - - 操作步驟
      - "`docs/how-to/<topic>.md`"
    - - 規格與欄位字典
      - "`docs/reference/<topic>.md`"
    - - 決策紀錄
      - "`docs/adr/` 或 `decisions/`"
    - - 本輪實驗／待驗證
      - "`.agent-goals/` 或 `notes/scratch/`（不當作單一真相）"
- name: 3. 檔案形狀（降低 token）
  source_line: 45
  allowed_actions:
  - action: 頂部 8–15 行「導航區」：目的、讀者、`kind`、主要依賴連結、**何時不必讀下去**。
    source: step
  - action: 索引優先：長主題拆成多 `leaf`；父檔只保留表格列「意圖 → 子檔路徑 → 讀取條件」。
    source: step
  - action: 少貼全文：程式碼、OpenAPI、長 log 用「路徑 + 節圍（行號或 anchor）」引用，不整段複製進規範文。
    source: step
  - action: 單一真相：同一規則不在兩處維護兩份全文；第二處只留一行連結與差異說明。
    source: step
  - action: 拆分閾值：見 [`../../governance/document-sizing.md`](../../governance/document-sizing.md)（行數與主題單一性）。
    source: step
- name: 4. 驗證與連動
  source_line: 53
  allowed_actions:
  - action: "**Link**：從本檔或 `README` 出去的相對連結可點、無斷鏈。"
    source: bullet
  - action: "**路由**：父層 README 的表格含新子檔；`skills-index` 或專案內索引若有對應列，一併更新。"
    source: bullet
  - action: "**Ai-skill 本庫**：若同步規則或模板，依 [`../../enforcement/dependency-reading.md`](../../enforcement/dependency-reading.md)
      與 [`../../enforcement/linked-updates.md`](../../enforcement/linked-updates.md)
      走完整閉環。"
    source: bullet
- name: 5. 完成訊號（給 agent 自查）
  source_line: 59
  allowed_actions:
  - action: "[ ] 新文件有 `kind` / `audience` / `stability`（或父 README 等價欄位）。"
    source: bullet
  - action: "[ ] 有 `index` 或導航區；長文已拆 `leaf`。"
    source: bullet
  - action: "[ ] 無重複規範全文；斷鏈已修。"
    source: bullet
  - action: "[ ] 本輪暫存與長期 durable 分界清楚。"
    source: bullet

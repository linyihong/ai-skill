# Runtime Guards

Canonical YAML: `circuit-breaker.yaml`, `context-pollution.yaml`.

`circuit-breaker.yaml` includes execution stability guards such as recursive depth, tool call limits, hallucination risk, `mismatch_escalation`, and the compressed cognitive-state primitive `execution_reliability_degradation`.

# Runtime Guards

Canonical YAML: `circuit-breaker.yaml`, `context-pollution.yaml`.

`circuit-breaker.yaml` includes execution stability guards such as recursive depth, tool call limits, hallucination risk, and `mismatch_escalation`.

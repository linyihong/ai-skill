id: models-small-model-checklist-first
domain: models
type: routing-decision
priority: P2
given:
  task: perform_low_risk_bounded_edit
  artifacts:
    - models/workflow-adaptation/small-model-workflows.md
    - models/compression/README.md
  signals:
    - task_is_low_risk
    - source_edit_not_required
when:
  action: choose_workflow_shape
then:
  expected_route:
    - use_checklist_first
    - record_deferred_sources
    - stop_at_validation_signal
  forbidden_routes:
    - skip_required_bootstrap
    - make_global_claim_from_checklist
  expected_final_route: checklist_first_bounded

#!/usr/bin/env bash
# Ai-skill：每次在本 repo commit 後，刷新本機 ~/.cursor bundles（略過失敗以免擋 commit）
ROOT="$(git rev-parse --show-toplevel 2>/dev/null)" || exit 0
SCRIPT="${ROOT}/scripts/sync-cursor-bundle.sh"
[[ -x "${SCRIPT}" ]] && "${SCRIPT}" || true

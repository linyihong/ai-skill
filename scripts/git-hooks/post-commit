#!/usr/bin/env bash
# Ai-skill：可選的 Cursor bundle refresh；reference-only 不需要同步。
ROOT="$(git rev-parse --show-toplevel 2>/dev/null)" || exit 0
SCRIPT="${ROOT}/scripts/sync-cursor-bundle.sh"
if [[ "${AI_SKILL_SYNC_CURSOR_BUNDLE:-0}" == "1" && -x "${SCRIPT}" ]]; then
  "${SCRIPT}" || true
fi

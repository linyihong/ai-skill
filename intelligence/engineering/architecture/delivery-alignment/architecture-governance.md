# Architecture Governance

**Status**: `candidate-intelligence`

Architecture governance 的任務是確保 complexity proportionality、rejected alternatives、minimality 與 validation plan。它不把 DDD tactical pattern promotion 成 runtime invariant。

# Reference Types (Illustrative)

**Status**: `candidate-intelligence`

**Not** a canonical classification. Do not grow this into a taxonomy, hierarchy, or ontology.

Purpose: quick orientation when choosing which attributes to extract in Step 2. The workflow under test is **Reference → Spec → Implement**, not «how many reference types exist».

```text
UI Reference         → layout / spacing / color
API Reference        → schema / constraints
Legacy System        → behavior / contract
Competitor Product   → workflow / capability
```

For UI screens, use the project **Visual Reference Spec** template instead of extending this file.
